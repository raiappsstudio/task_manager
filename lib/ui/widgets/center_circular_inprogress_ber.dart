import 'package:flutter/material.dart';

class CenterCirculerInprogressBer extends StatelessWidget {
  const CenterCirculerInprogressBer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
